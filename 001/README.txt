My goal was to, again, try to do TDD in Go. I didn't finish the file reading, but it's working well enough as a library. I used a new TDD/BDD runner called GoConvey.

I left an extra implementation of an iterator to show some of the odd ways that Go allows you to make things parallel.

